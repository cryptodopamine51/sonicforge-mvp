## Packages
framer-motion | Smooth animations for list items and page transitions
lucide-react | Beautiful icons for the UI
date-fns | Formatting timestamps relative to now

## Notes
The app requires polling for job status updates.
Using standard fetch/TanStack Query for data fetching.
Dark mode is the default aesthetic.
